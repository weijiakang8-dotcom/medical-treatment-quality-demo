# 合成有提示/无提示对照报告

> 合成行为模拟，不是真实用户测试，不用于推断提示会改变人的行为。

## 1. 观测到的系统输出

将T4完全相同的4条事件输入两种模式：

| 比较项 | 无提示模式 | 有提示模式 |
|---|---:|---:|
| 输入事件是否相同 | 是 | 是 |
| 总操作事件 | 4 | 4 |
| AlertEvent | 0 | 1 |
| 原始事件结构 | 保留 | 保留 |
| 质量日志 | 生成 | 生成并包含提示 |

系统观测：有提示模式能在相同疑似禁区事件下额外生成1条 `AlertEvent`；两种模式没有发生人的行为变化，因此原始偏差相同。

## 2. 合成假设

对T2增加一条明确标记为 `syntheticAssumption: received_alert_then_corrected` 的区域C完成事件，假设“收到提示后执行修正”。这条事件由测试脚本人为添加，不来自用户、医生、设备或临床场景。

## 3. 合成模拟结果

| 指标 | 修正前错误轨迹 | 合成修正后轨迹 |
|---|---:|---:|
| 覆盖率 | 67% | 100% |
| 区域偏差率 | 33.3% | 0% |

该结果只证明：如果补充缺失区域的完成事件，确定性公式会相应更新。

## 4. 不可推断的内容

- 不能推断真实操作者会理解、接受或执行提示。
- 不能推断提示会减少真实操作偏差或不会打断工作。
- 不能推断视频识别能够正确发现偏差。
- 不能推断操作质量指标与疗效、安全、客诉或复购相关。
- 不能推断半岛医疗系统支持这些字段或接口。
