# V2沙盒测试报告

> `[合成测试数据]` V2测试数据由本地脚本生成，不代表真实用户、医生、机构、患者、设备或临床结果。本地沙盒压力测试，不代表生产环境性能。

## 验收结果

18/18通过：注册、登录、登出、统一登录失败信息、权限拒绝、机构隔离、患者本人作用域、设备CSV导入、无效字段、视频任务、模拟AI标记、ROI公式、空NPS、演示NPS、审计、导出、删除和V1/V2键隔离。

## 权限和租户边界

- Demo Clinic A管理员只能列出A机构患者，B机构患者不可见。
- 患者角色只返回绑定 `demoPatientId` 的治疗对象，且没有创建治疗权限。
- 普通角色调用平台管理动作会被 `AuthService.require()` 拒绝并写入权限拒绝审计。
- 登出清除 `v2_auth` 会话；无会话时权限判断失败。

以上仅证明沙盒服务逻辑。客户端权限不是生产安全边界，可被开发者工具绕过。

## 适配器结果

- 合法示例CSV转换为统一 `DeviceEvent`，来源为 `mock_device`；缺少必填字段时返回字段错误。
- 视频任务只校验MP4/WebM和20MB上限并建立状态，不上传或识别内容。
- RuleBasedProvider输出 `[规则生成]`；MockAIProvider输出 `[模拟AI结果]`；FutureVisionProvider返回 `[待真实模型验证]` 和 `not_implemented`。
- 所有AI对象带 `source`、`confidence`、`explanation`、`isSynthetic`、`requiresHumanReview`。

## ROI与NPS

ROI公式单元测试通过，示例只用于公式断言，页面默认字段为空，所有结果标记为情景测算。无评分时NPS返回“暂无数据”；明确演示评分返回 `[演示数据]`，患者和机构组分开计算。

## 规模结果

| 类型 | 数量 | 用时 | 状态 |
|---|---:|---:|---|
| 演示用户 | 100 | 0.17ms | 通过 |
| 演示机构 | 10 | 0.04ms | 通过 |
| 治疗记录 | 1,000 | 1.37ms | 通过 |
| 操作事件 | 10,000 | 8.54ms | 通过 |
| 审计日志 | 1,000 | 1.12ms | 通过 |

数值只反映一次Node内存序列化运行，未覆盖浏览器容量、并发、网络、数据库事务或生产负载。

## V1隔离

V2键全部以 `v2_` 开头；测试确认没有写入 `demo_state`、`treatment_session` 或 `validation_records`。V1规则12/12、轨迹5/5、作品集检查10/10和V1压力测试全部保持通过。
