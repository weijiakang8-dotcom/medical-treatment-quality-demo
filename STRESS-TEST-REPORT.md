# V1本地工程压力测试报告

> `[合成测试数据]` 本报告数据由 `stress.test.mjs` 固定规则生成，不代表真实用户、医生、设备、机构或临床结果。本地合成数据下的工程压力测试结果，不代表真实机构部署性能。

## 汇总

| 类别 | 结果 | 说明 |
|---|---:|---|
| S1—S18边界轨迹 | 18/18通过 | 正常、重复、乱序、缺失、未知、非法、超长、撤销及会话重置 |
| 规模测试 | 4/4通过 | 10、100、1,000、10,000事件 |
| 失败安全 | 12/12通过 | 非法事件被校验器拒绝，不进入日志统计 |
| 数据一致性 | 7/7通过 | 事件计数、撤销提示、重复ID、规则版本、存储隔离和CSV字段 |

## 规模结果

| 合成事件数 | 完成 | 用时 | 日志事件数 | 覆盖率 | 导出 |
|---:|---|---:|---:|---:|---|
| 10 | 是 | 13.55ms | 10 | 100% | 正常 |
| 100 | 是 | 0.12ms | 100 | 100% | 正常 |
| 1,000 | 是 | 0.78ms | 1,000 | 100% | 正常 |
| 10,000 | 是 | 8.92ms | 10,000 | 100% | 正常 |

首次10事件含Node启动/预热影响，因此不用于横向性能推断。未采集可靠的进程级内存基线，不作内存承诺。

## 失败安全

- 缺失或未知 `regionId`、非法 `eventType`、缺失时间戳、重复ID和超过2,000字符的附加字段由独立 `OperationEventValidator` 拒绝。
- 重复ID采用“首条有效事件保留、后续重复拒绝”，不重复计入日志。
- 空计划和空事件返回可解释的0或不可计算指标，不崩溃。
- 刷新、退出演示、快速点击、重复导出和存储隔离由现有作品集测试与模块契约覆盖。

## 测试发现与修复

首次运行发现 `TreatmentPlan` 和 `QualityLog` 未保存规则版本，无法证明旧日志对应原始规则。已增加 `ruleVersion` 计划快照和日志字段，修复后数据一致性7/7通过。

## 边界

这些结果允许说明V1规则引擎在本地合成事件下可完成边界处理、批量回放和日志输出；不能说明生产吞吐、并发能力、真实设备接入、真实操作者行为、临床安全有效性或商业结果。
