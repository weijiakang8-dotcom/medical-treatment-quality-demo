# V2沙盒数据治理

> V2扩展功能仅用于沙盒演示与架构验证，不接触真实患者、不用于真实医疗决策、不连接未经授权的设备或内部系统。

## 数据分类

| 分类 | 示例 | 来源标签 | 允许用途 |
|---|---|---|---|
| 项目配置 | 角色、权限、Repository键 | `[项目配置数据]` | 架构与权限测试 |
| 沙盒演示 | Demo Clinic A/B、匿名患者编号、演示账号 | `[沙盒演示数据]` | 本地页面演示 |
| 合成测试 | 100用户、1,000治疗、10,000事件 | `[合成测试数据]` | 本地规模与隔离测试 |
| 用户输入 | ROI变量、手工NPS | `[用户输入]` | 当前浏览器情景计算 |
| 规则输出 | 事件分类、解释文案 | `[规则生成数据]` | 接口演示，需人工复核 |
| 未获取真实数据 | 患者、设备、视频、内部经营数据 | `[待真实环境验证]` | 当前禁止采集和使用 |

## 沙盒边界与脱敏

- 患者只允许演示姓名或匿名编号；拒绝明显手机号、身份证/病历号提示。
- 不保存或处理真实面部照片、医疗记录、诊断、疗效或安全结论。
- 视频模块只记录合成文件元数据，不读取、上传或分析视频内容。
- 设备模块只处理本地示例CSV，不控制设备、不发送指令。
- AI Provider仅生成规则解释或模拟摘要，输出均要求人工复核。

## 保留与删除

数据保存在当前浏览器 `v2_*` 本地键中，直到用户执行删除、清理站点数据或浏览器策略清除。当前机构删除会删除带该 `organizationId` 的沙盒业务对象；全量删除可通过清除站点数据完成。删除不是生产级不可恢复擦除，浏览器备份、同步和设备层行为不在本原型控制范围内。

## 权限原则

- 预置 `Demo Admin` 由页面初始化规则生成，属于 `[项目配置数据]`，不是用户注册或真实账号。
- 所有页面动作通过独立 `AuthService.require()` 检查。
- 非平台管理员不能管理平台机构；组织管理员只作用于本机构。
- 患者作用域按 `demoPatientId` 过滤；前台不读取医学操作详情。
- 权限拒绝、登录、登出、创建、导入、上传、AI生成、查看、导出和删除写入审计日志。

## 当前不满足生产部署要求

- 客户端身份和权限可被开发者工具绕过，没有可信服务端执行边界。
- FNV样式本地哈希仅避免明文展示，不是带盐慢哈希或生产密码存储。
- `localStorage`不是数据库，不支持事务、并发、可靠备份、加密、租户行级安全或灾备。
- 审计日志可被本地用户篡改或清除，不具备不可抵赖性。
- 没有真实身份核验、会话过期、CSRF/XSS系统防护、密钥管理和安全审计。
- 没有个人信息影响评估、数据处理协议、伦理/医学/法规确认。
- 没有真实设备协议验证、医学视频模型、临床验证或生产运维监控。

进入真实部署前必须将Auth、Repository、审计、文件和适配器替换为经授权的服务端能力，并完成产品、医学、法规、隐私、信息安全和质量体系评估。
