# 部署验证报告

## 当前状态

- 目标平台：Vercel。
- 部署时间：待实际发布后记录。
- 公网地址：待实际发布后记录。
- 前端构建命令：无，项目为原生静态HTML/CSS/JavaScript。
- Serverless目录：`api/ai/`。
- 数据模式：浏览器本地演示数据。

## API路径

- `GET /api/ai/status`
- `POST /api/ai/plan`
- `POST /api/ai/summary`
- `POST /api/ai/query`

## 环境变量

- `LLM_BASE_URL`
- `LLM_API_KEY`
- `LLM_MODEL`
- `LLM_TIMEOUT_MS`
- `DEMO_MODE`

真实密钥不写入本文档、源代码或前端包。

## 部署前验证

- V1：68/68通过。
- V2：23/23通过。
- 统一整合：12/12通过。
- 既有回归合计：103/103通过。
- AI边界：16/16通过，使用22条合成夹具。
- Serverless部署安全：10/10通过。
- 前端质量：12/12通过。
- 密钥扫描：未在可部署文件中发现本地真实密钥。
- 公开运行时地址：非本地主机使用同源`/api/ai`；本机地址只存在于明确的本地开发分支。

以上结果是工程与边界验证，不是医学准确率、医生测试或临床验证。

## 部署后验收

待公网发布后实际记录以下结果：

- 首页与`/v2/`直达和刷新。
- 主流程从新建记录到质量报告。
- 计划、摘要和查询的真实LLM调用。
- 智能生成、待人工确认、演示数据和非医疗决策提示。
- 敏感输入拒绝、模型错误和手动降级。
- 浏览器控制台与网络错误。

## 已知限制

- 当前没有生产数据库、正式身份认证、真实设备、图片或视频分析。
- 内存速率限制按Serverless实例工作，不是分布式严格限流。
- AI可用性依赖配置的OpenAI兼容供应商与模型池。
- 作品集没有独立`/portfolio/`HTML入口；说明位于主站“关于本系统”和仓库文档。
- 未完成医疗、法规、临床、隐私合规或生产安全认证。
