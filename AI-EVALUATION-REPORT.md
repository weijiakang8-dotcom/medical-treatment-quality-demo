# AI评估报告

## 数据边界

全部输入均为 `[合成测试数据]`，不代表真实医生、患者或机构输入。评估结果不是医学准确率、临床有效性或真实用户满意度。

## 配置与真实调用

- 接口：OpenAI兼容接口，经本地Node代理调用。
- 模型：`gpt-5.3-codex-spark`。
- 输入：仅使用脱敏合成文字和结构化演示记录。
- 原始输入留存：不保存。
- 人工确认：所有成功结果均为`true`。

实际调用结果：

| 能力 | 最终真实调用 | Schema校验 | 人工确认 | 说明 |
|---|---:|---:|---:|---|
| 计划草稿 | 成功 | 通过 | 保留 | 区域ID匹配当前配置 |
| 质量摘要 | 成功 | 通过 | 保留 | 未修改原始质量日志 |
| 查询解析 | 成功 | 通过 | 保留 | 仅生成白名单只读条件 |

首次尝试的模型池返回HTTP 429限流，系统正确降级；切换到可用文本模型后成功。真实调用过程中也出现过Schema拒绝，系统未采用该输出，之后通过严格结构化响应成功。

## 确定性评估

- 合成测试输入：22条。
- 自动测试：16项。
- 通过：16项。
- JSON与Schema路径：通过。
- 未知区域拦截：通过。
- 模糊表达澄清机制：已在Schema和Prompt中要求。
- 医疗越界内容拦截：通过。
- 敏感信息拦截：通过。
- 失败降级：通过。
- 人工确认字段保留：通过。
- 审计不保存输入正文：通过。

## 已知限制

真实模型输出具有波动性；即使使用结构化响应，也必须经过本地Schema、业务规则和人工确认。当前评估不包含真实医生、真实患者、真实设备、照片或视频。
