# AI公开部署边界

## 允许能力

公开Demo中的真实LLM只用于：

1. 将非敏感自然语言整理为治疗计划草稿。
2. 将已有质量日志整理为事实摘要。
3. 将自然语言问题解析为白名单只读查询条件。

所有结果必须经过Schema、敏感信息、医疗越界和业务白名单校验，并由用户人工确认。

## 禁止能力

LLM不得直接读取或写入浏览器存储、数据库或文件，不得执行SQL或代码，不得提交治疗计划，不得修改原始质量日志、确定性检测结论或指标。不得进行诊断、疗效预测、安全判断、治疗适宜性判断、医学定位、治疗参数推荐、设备控制或法规判断。

## 数据边界

只允许匿名编号、合成区域记录、待确认事项、提醒和时长。禁止真实姓名、手机号、身份证号、病历号、人脸图片、视频、真实病历、半岛内部材料和未授权设备数据。

Serverless函数不持久化输入和模型输出。平台运行日志应仅用于技术状态排查，不应主动记录请求正文。

## 失败行为

未配置、超时、供应商错误、非法JSON、空输出、Schema失败、敏感信息或医疗越界均返回统一安全错误。前端继续提供手动选择区域、记录操作、规则提醒和质量报告。

## 非生产限制

本实现没有多区域集中式速率限制、正式身份认证、生产数据库、密钥轮换自动化、合规审计平台、医疗设备连接或临床验证。它只能作为公开产品原型演示。
