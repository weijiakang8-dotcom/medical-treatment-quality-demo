# 第4页｜解决方案与MVP原型

## 超声治疗操作质量哨兵

> 模拟验证原型，不用于真实医疗决策；未接入半岛设备、数据或系统。

**主用户故事：** 作为超声治疗操作医生，我希望在操作前确认允许区域和预设禁区，在模拟过程中获得疑似漏打或越界提示，并在结束后得到可复盘日志。

**机构用户故事：** 作为机构负责人或运营人员，我希望查看单次模拟的覆盖、偏差和事件记录，用于发现待复核流程问题。该价值尚未经过目标用户验证。

## 核心流程

```text
治疗计划 → 区域规则确认 → 人工模拟操作 → 偏差提示 → 一页质量日志 → TXT/CSV导出
```

## AI能力边界与真实调用

新增低风险文本链路：`自然语言 → 真实LLM结构化草稿 → Schema与区域规则校验 → 人工确认 → 既有治疗流程`。AI仅整理计划草稿、质量记录摘要和只读查询条件，不执行医疗判断，也不修改确定性检测与指标逻辑。当前通过本地Node代理调用OpenAI兼容接口，配置模型为`gpt-5.3-codex-spark`；真实三项调用最终均成功并通过Schema，原始输入不写入审计日志。模型不可用时手动流程完整保留。

原型：`http://127.0.0.1:4173/`（本机运行地址）

截图：`screenshots/01-entry.png` 至 `screenshots/07-quality-log.png`覆盖V1入口、计划、规则、进行中、漏打、疑似越界和日志；`screenshots/08-v2-sandbox.png`展示V2沙盒身份与导航。

## 解耦模块

| 模块 | 职责 | 当前实现 | 未来替换 |
|---|---|---|---|
| TreatmentPlanModule | 标准计划对象 | 表单 | 机构计划适配器 |
| RegionRuleModule | 允许区域/禁区规则 | JSON配置 | 内部受控配置服务 |
| InputModule | 输入转事件 | 人工按钮 | 视频或设备日志适配器 |
| OperationEventModule | 记录、撤销、隔离 | 内存+localStorage | 受控日志服务 |
| DeviationDetector | 可解释规则检测 | JavaScript确定性规则 | 内部确认的规则引擎 |
| AlertModule | 提示生命周期 | 本地AlertEvent | 审阅工作流 |
| QualityLogModule | 聚合质量日志 | 浏览器计算 | 后端日志服务 |
| ExportModule | 输出日志 | TXT/CSV | 受控归档接口 |

统一对象：`TreatmentPlan`、`RegionRule`、`OperationEvent`、`AlertEvent`、`QualityLog`。页面不直接承载规则；输入来源可替换而不改变检测和日志接口。

## 实现状态

| 已实现 | 模拟实现 | 未实现 |
|---|---|---|
| 治疗计划；配置化区域规则；允许/禁区展示；操作事件；漏打检测；疑似越界提示；覆盖率；区域偏差率；偏差事件率；质量日志；TXT/CSV导出；本地数据隔离；12项自动测试；5条轨迹回放 | 人工区域点击；模拟进入禁区；合成操作轨迹；人为添加的合成修正事件 | 视频识别；真实设备接入；真实法规校验；真实医学定位；临床应用；真实操作者测试；半岛内部接口 |

## V2沙盒扩展与生产化前置条件

V2位于独立 `v2/` 目录，使用可替换Repository、Auth、DeviceAdapter、VideoJob和AIProvider接口。V1仍是唯一业务主线；`integration.js`中的EventNormalizer、TreatmentWorkflowService和TreatmentContextService只负责输入标准化与ID引用。统一分层和流向见 `UNIFIED-ARCHITECTURE.md`、`UNIFIED-DATA-FLOW.md`。`[工程验证]` 已实现本地沙盒账号/RBAC、演示机构隔离、匿名患者只读页、模拟CSV、视频任务状态、规则与模拟AI输出、ROI/NPS公式、审计及删除导出。所有数据使用 `v2_*` 键，不读取V1。

`[待真实环境验证]` 真实认证、服务器端租户隔离、云数据库、真实患者/设备/视频、医学模型、临床与法规、生产安全和灾备均未实现。

**指标边界：** 漏打是结束时根据缺少完成事件推断的“疑似漏打区域”；越界是人工触发的“疑似进入预设禁区”，均需人工复核。
